export const APP_CONSTANTS = {
  DEFAULT_PAGE_SIZE: 10,
  REQUEST_TIMEOUT_MS: 30000,
} as const;

export const ROUTES = {
  home: '/',
  genres: '/genres',
  authors: '/authors',
  books: '/books',
  login: '/login',
} as const;

