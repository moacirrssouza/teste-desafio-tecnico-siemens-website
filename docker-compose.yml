services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL}
    ports:
      - '5000:80'
    environment:
      - VITE_API_BASE_URL=${VITE_API_BASE_URL}
